import React from 'react';
export type PaymentInfoFormProps = {
    type: string;
    postsId: string;
    total: string;
};
export declare const PaymentInfoForm: ({ type, postsId, total, }: PaymentInfoFormProps) => React.JSX.Element;
